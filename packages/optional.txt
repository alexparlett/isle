# Optional extras that were considered and NOT taken. Left here as a record of
# what was looked at, so the same audit doesn't get redone from scratch.
#
# The groups that were taken live in packages/extras.txt and install.sh
# installs them. To add one of these later:
#
#     sudo pacman -S --needed obs-studio xpadneo-dkms

[gaming]
# Everything the gaming stack actually needs is already installed or in
# repo.txt: steam, lutris, heroic, gamescope, gamemode, mangohud, goverlay,
# wine, winetricks, protontricks, coolercontrol, and the full Vulkan stack for
# both the RTX 5070 Ti and the iGPU. These are the extras beyond that.
obs-studio          # recording and streaming; wf-recorder only does plain capture
xpadneo-dkms        # Xbox controllers over Bluetooth, properly
# game-devices-udev # AUR: broad controller support (PS, Switch Pro, 8BitDo)
# vkbasalt          # AUR: post-processing (sharpening, CAS) for Vulkan titles
# corectrl          # AMD-focused GPU tuning; coolercontrol already covers fans
# steamtinkerlaunch # AUR: per-game launch wrapper GUI

[desktop]
cups                # there is no printing stack installed at all
system-config-printer
flatpak             # escape hatch for anything not packaged for Arch
bluez-obex          # sending files over Bluetooth
easyeffects         # EQ and per-app audio effects on PipeWire
qpwgraph            # visual PipeWire patchbay, invaluable when routing breaks
# thunderbird       # Proton Mail's desktop app is already installed
# keepassxc         # Proton Pass covers this
# syncthing         # only if you sync between machines
