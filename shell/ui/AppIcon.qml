import QtQuick
import Quickshell.Widgets

// An application's icon from the icon theme, by resolved path.
IconImage {
    property int size: 20
    implicitSize: size
    width: size
    height: size
}
